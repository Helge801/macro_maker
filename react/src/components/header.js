import React from 'react';
import '../stylesheets/header.css';

const Header = (props) => {
  return(
    <div className="header-wrapper">
        <h3 className="header-title">Pseudo-Code to CF-Macro</h3>
    </div> 
  )
}

export default Header;
